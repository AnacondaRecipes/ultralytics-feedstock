{% set name = "ultralytics" %}
{% set version = "8.3.225" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.org/packages/source/{{ name[0] }}/{{ name }}/{{ name }}-{{ version }}.tar.gz
  sha256: d757ac2d040e6195c187eb65bb58d36b03d99ac9dac47ec48f0bc55d027b91d2

build:
  script: {{ PYTHON }} -m pip install . -vv --no-deps --no-build-isolation
  number: 0
  skip: true  # [py<38]
  entry_points:
    - yolo = ultralytics.cfg:entrypoint
    - ultralytics = ultralytics.cfg:entrypoint

requirements:
  host:
    - python
    - pip
    - setuptools >=70.0.0
    - wheel
  run:
    - python
    - numpy >=1.23
    - matplotlib-base >=3.3.0
    - opencv >=4.6.0
    - pillow >=7.1.2
    - pyyaml >=5.3.1
    - requests >=2.23.0
    - scipy >=1.4.1
    - pytorch >=1.8.0  # [unix]
    - pytorch >=1.8.0,!=2.4.0  # [win]
    - torchvision >=0.9.0 
    - psutil
    - polars
    - ultralytics-thop >=2.0.0
  run_constrained:
    # export
    - numpy <2.0.0
    - onnx >=1.12.0  # [not osx]
    - onnxruntime >=1.12.0,<1.18.0  # [osx]
    - coremltools >=8.0  # [not win and py<=313]
    - scikit-learn >=1.3.2  # [not win and py<=313]
    - openvino >=2024.0.0
    - tensorflow >=2.0.0,<=2.19.0
    - tensorflowjs >=2.0.0
    - tensorstore >=0.1.63  # [aarch64 and py>=39]
    - h5py !=3.11.0  # [aarch64]
    # solutions
    - shapely >=2.0.0
    - streamlit >=1.29.0
    - flask >=3.0.1
    # extra
    - hub-sdk >=0.0.12
    - albumentations>=1.4.6
    - faster-coco-eval >=1.6.7

test:
  source_files:
    - tests
  imports:
    - ultralytics
  requires:
    - pip
    - pytest
  commands:
    - pip check
    - python -c "from importlib.metadata import version; assert(version('{{ name }}')=='{{ version }}')"
    - pytest -v tests
    - yolo checks
    - ultralytics version

about:
  home: https://ultralytics.com
  summary: Ultralytics YOLO for SOTA object detection, multi-object tracking, instance segmentation, pose estimation and image classification.
  license: AGPL-3.0-only
  license_family: GPL
  license_file: LICENSE
  description: |
    Ultralytics creates cutting-edge, state-of-the-art (SOTA) YOLO models built on years of
    foundational research in computer vision and AI. Constantly updated for performance and
    flexibility, our models are fast, accurate, and easy to use. They excel at object detection, tracking,
    instance segmentation, image classification, and pose estimation tasks.
  doc_url: https://docs.ultralytics.com
  dev_url: https://github.com/ultralytics/ultralytics

extra:
  recipe-maintainers:
    - onuralpszr
    - UltralyticsAssistant
    - glenn-jocher
    - Laughing-q
    - pderrenger
